import { GeneratorConfig } from "../types";

export const dockerfileGeneratorConfig: GeneratorConfig = {
  title:
    "Generate a deployment-ready Dockerfile for Next.js applications in the monorepo.",
  command: "dockerfile",
  params: [],
};
